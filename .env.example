# Backend
PORT=8787
COINGECKO_API_BASE=https://api.coingecko.com/api/v3
ALLOWED_ORIGIN=*
BACKUP_DIR=/data/backups

# Frontend — required for GitHub Pages builds via repo variable VITE_API_BASE_URL
# Local dev: leave empty (Vite proxies /api to localhost:8787)
# VITE_API_BASE_URL=https://api-nimconnect.example.com
